/* Automatisch erzeugt von chap_hypothesen/build.py -- nicht von Hand
   aendern. Quelle: code/gyminf/python/hypothesen/ (Submodul gyminf) */
window.EXAMPLES = {
 "hypothesen/abtest.py": "import numpy as np\nfrom scipy import stats\n\n# Zeile 1: Layout A, Zeile 2: Layout B; Spalte 1: gekauft, Spalte 2: nicht gekauft\ntabelle = np.array([[120, 880],\n                     [158, 842]])\n\nchi2, p_val, dof, erwartet = stats.chi2_contingency(tabelle, correction=False)\nprint(f\"Conversion A: {120/1000:.1%}, Conversion B: {158/1000:.1%}\")\nprint(f\"chi2 = {chi2:.3f}, p-Wert = {p_val:.4f}\")\n",
 "hypothesen/bildschirmzeit.py": "import numpy as np\nfrom scipy import stats\n\ndaten = np.array([210, 245, 190, 260, 300, 175, 220, 255,\n                   230, 205, 280, 195, 240, 265, 215])\n\nergebnis = stats.bootstrap((daten,), np.mean, confidence_level=0.95,\n                            n_resamples=10000, method='percentile')\n\nprint(f\"Mittelwert: {daten.mean():.1f} Minuten\")\nprint(f\"95%-Konfidenzintervall: {ergebnis.confidence_interval}\")\n",
 "hypothesen/esport.py": "import numpy as np\nfrom scipy import stats\n\nmaus       = np.array([210, 198, 205, 215, 190, 200, 208, 195])\ncontroller = np.array([230, 225, 240, 220, 235, 245, 228, 232])\n\nt_stat, p_val = stats.ttest_ind(maus, controller, alternative='less')\nprint(f\"Mittel Maus: {maus.mean():.1f}ms, Mittel Controller: {controller.mean():.1f}ms\")\nprint(f\"t = {t_stat:.3f}, p-Wert = {p_val:.6f}\")\n",
 "hypothesen/lootbox.py": "import numpy as np\nfrom scipy import stats\n\nbeobachtet = np.array([50, 38, 45, 42, 25])\nerwartet = np.full(5, beobachtet.sum() / 5)  # je 40 erwartet\n\nchi2, p_val = stats.chisquare(beobachtet, erwartet)\nprint(f\"chi2 = {chi2:.3f}, p-Wert = {p_val:.4f}\")\n",
 "hypothesen/montecarlo.py": "import numpy as np\nfrom scipy import stats\n\nrng = np.random.default_rng()\nn, k, p_null = 100, 62, 0.5\nanzahl_simulationen = 200_000\n\nsimulationen = rng.binomial(n, p_null, size=anzahl_simulationen)\nextrem = np.abs(simulationen - n * p_null) >= abs(k - n * p_null)\np_simuliert = np.mean(extrem)\nprint(f\"Simulierter p-Wert: {p_simuliert:.4f}\")\n\np_exakt = stats.binomtest(k, n, p=p_null, alternative='two-sided').pvalue\nprint(f\"Exakter p-Wert:     {p_exakt:.4f}\")\n",
 "hypothesen/schritte.py": "import numpy as np\nfrom scipy import stats\n\nschritte = np.array([9800, 9200, 10100, 8950, 9400,\n                      9600, 9300, 10050, 8800, 9500])\n\nt_stat, p_val = stats.ttest_1samp(schritte, popmean=10000, alternative='less')\nprint(f\"Mittelwert: {schritte.mean():.1f} Schritte\")\nprint(f\"t = {t_stat:.3f}, p-Wert = {p_val:.4f}\")\n",
 "hypothesen/vokabelapp.py": "import numpy as np\nfrom scipy import stats\n\nvorher  = np.array([11, 9, 14, 8, 12, 10, 13, 9, 11, 10])\nnachher = np.array([13, 12, 15, 10, 14, 12, 14, 12, 13, 12])\n\nt_stat, p_val = stats.ttest_rel(nachher, vorher, alternative='greater')\nprint(f\"Mittlere Verbesserung: {(nachher - vorher).mean():.2f} Punkte\")\nprint(f\"t = {t_stat:.3f}, p-Wert = {p_val:.6f}\")\n"
};
