/* Automatisch erzeugt von chap_regression/build.py -- nicht von Hand
   aendern. Quelle: code/gyminf/python/regression/ (Submodul gyminf) */
window.EXAMPLES = {
 "dynamic-systems/matplotlib-example.py": "import matplotlib.pyplot as plt\n\nx = [0, 0, 100, 200, 200]\ny = [0, 100, 0, 100, 0]\nplt.plot(x,y, label=\"M curve\")\nz = [75,100,125]\nw = [80, 80, 80]\nplt.scatter(z,w,s=100,color=\"red\",label=\"Red dots\")\nplt.legend()\nplt.show()\n\n",
 "regression/auto.py": "import numpy as np\nimport matplotlib.pyplot as plt\n\nkm = np.array([15, 38, 52, 71, 96, 120])\npreis = np.array([24.5, 20.8, 18.9, 15.4, 12.1, 9.0])\n\nm, b = np.polyfit(km, preis, 1)\nr = np.corrcoef(km, preis)[0, 1]\nprint(f\"(a) y = {m:.4f}x + {b:.4f}, r = {r:.4f}\")\n\n# (b) Steigung: Preisänderung pro 1000 km. y-Achsenabschnitt: Preis bei km=0.\nprint(f\"(b) Preis sinkt um {abs(m) * 1000:.0f} CHF pro 1000 km; Neupreis ≈ {b * 1000:.0f} CHF\")\n\n# (c) Vorhersagen\nfor kilometerstand in [60, 150]:\n    print(f\"(c) {kilometerstand}'000 km -> {m * kilometerstand + b:.3f} '000 CHF\")\n\n# (d) Kilometerstand, bei dem der Preis auf 0 sinkt\nprint(f\"(d) gratis ab {-b / m:.2f} '000 km\")\n\nplt.scatter(km, preis, color=\"red\", label=\"Occasionen\")\nxs = np.array([0, 180])\nplt.plot(xs, m * xs + b, color=\"blue\", label=\"Regressionsgerade\")\nplt.xlabel(\"Kilometerstand [1000 km]\")\nplt.ylabel(\"Preis [1000 CHF]\")\nplt.legend()\nplt.show()\n",
 "regression/bremsquad.py": "import numpy as np\nimport matplotlib.pyplot as plt\n\nv = np.array([30, 50, 70, 90, 110, 130])\ns = np.array([6, 15, 29, 48, 72, 100])\n\n# (a) Lineare Regression\nm, b = np.polyfit(v, s, 1)\nr = np.corrcoef(v, s)[0, 1]\nprint(f\"(a) linear: s = {m:.4f}v + {b:.4f}, r = {r:.4f}\")\n\nplt.scatter(v, s, color=\"red\", label=\"Messdaten\")\nplt.xlabel(\"Geschwindigkeit [km/h]\")\nplt.ylabel(\"Bremsweg [m]\")\n\n# (b) Quadratische Regression\na2, b2, c2 = np.polyfit(v, s, 2)\nprint(f\"(b) quadratisch: s = {a2:.5f}v² + {b2:.5f}v + {c2:.5f}\")\n\nvs = np.linspace(25, 135, 200)\nplt.plot(vs, m * vs + b, color=\"grey\", linestyle=\"dashed\", label=\"linear\")\nplt.plot(vs, a2 * vs**2 + b2 * vs + c2, color=\"blue\", label=\"quadratisch\")\nplt.legend()\nplt.show()\n\n# (c) Vorhersagen fuer v=160\nprint(f\"(c) linear: {m * 160 + b:.2f} m, quadratisch: {a2 * 160**2 + b2 * 160 + c2:.2f} m\")\n\n# (d) Summe der Residuenquadrate, zum Vergleich der Modelle\nssr_lin = np.sum((s - (m * v + b)) ** 2)\nssr_quad = np.sum((s - (a2 * v**2 + b2 * v + c2)) ** 2)\nprint(f\"(d) SSR linear = {ssr_lin:.4f}, SSR quadratisch = {ssr_quad:.4f}\")\n",
 "regression/bremsweg-alter.py": "import numpy as np\n\nalter = np.array([4, 7, 11, 2])\nbremsweg = np.array([50, 80, 70, 45])\n\nm, b = np.polyfit(alter, bremsweg, 1)\nprint(f\"s(a) = {m:.4f} * a + {b:.4f}\")\n\nfor jahre in [0, 15, 30]:\n    print(f\"Alter {jahre}: {m * jahre + b:.2f} m\")\n# Alter 0:  41.68 m\n# Alter 15: 90.60 m\n# Alter 30: 139.51 m\n",
 "regression/fuenf-punkte-r0.py": "import numpy as np\n\n# Ein Beispiel von 5 Punkten, die nicht auf einer Geraden liegen, aber\n# trotzdem den empirischen Korrelationskoeffizienten r = 0 ergeben.\nx = np.array([0, 1, 2, -1, -2])\ny = np.array([0, 1, 4, 1, 4])\n\nr = np.corrcoef(x, y)[0, 1]\nprint(\"r =\", r)\n",
 "regression/hand1.py": "import numpy as np\n\nx = np.array([0, 1, 3])\ny = np.array([0, 2, 1])\n\n# (a) Summe der Residuenquadrate fuer die vorgegebene Gerade y = 0.9x - 1\nm_guess, b_guess = 9 / 10, -1\nresiduen = y - (m_guess * x + b_guess)\nprint(\"(a) Summe Residuenquadrate:\", np.sum(residuen**2))  # 5.9\n\n# (b) Regressionsgerade und ihre Summe der Residuenquadrate\nm, b = np.polyfit(x, y, 1)\nprint(f\"(b) Regressionsgerade: y = {m:.4f}x + {b:.4f}\")  # y = 0.2143x + 0.7143\n\nresiduen_reg = y - (m * x + b)\nprint(\"(b) Summe Residuenquadrate:\", np.sum(residuen_reg**2))  # 1.7857\n",
 "regression/kepler.py": "import numpy as np\n\na = np.array([0.387, 0.723, 1.000, 1.524, 5.203, 9.537])  # Sonnenabstand [AE]\nT = np.array([0.241, 0.615, 1.000, 1.881, 11.862, 29.457])  # Umlaufzeit [Jahre]\n\n# Potenzregression T = c * a^n: linearisieren mit ln(T) = n*ln(a) + ln(c)\nn, lnc = np.polyfit(np.log(a), np.log(T), 1)\nc = np.exp(lnc)\nr = np.corrcoef(np.log(a), np.log(T))[0, 1]\nprint(f\"(a) T = {c:.2f} * a^{n:.4f}, r = {r:.6f}\")\n\n# (b) Der Exponent liegt sehr nahe bei 3/2 -> T^2 ≈ a^3 (3. Keplersches Gesetz)\nprint(f\"(b) Exponent ≈ 3/2 = {3 / 2}\")\n\n# (c) Umlaufzeit von Neptun (a = 30.07 AE)\nneptun = c * 30.07**n\nprint(f\"(c) Neptun: T ≈ {neptun:.2f} Jahre\")\n",
 "regression/koffein.py": "import numpy as np\n\nt = np.arange(0, 7)\nc = np.array([120, 104, 89, 78, 67, 58, 50])\n\nk, lna = np.polyfit(t, np.log(c), 1)\na = np.exp(lna)\nq = np.exp(k)\nprint(f\"(a) c(t) = {a:.2f} * {q:.2f}^t\")\n\n# (b) Prozentuale Abnahme pro Stunde\nprint(f\"(b) Abnahme: {(1 - q) * 100:.1f} % pro Stunde\")\n\n# (c) Halbwertszeit: a*q^t = a/2  =>  t = ln(0.5)/ln(q)\nhalbwertszeit = np.log(0.5) / k\nprint(f\"(c) Halbwertszeit: {halbwertszeit:.2f} h\")\n\n# (d) Zeit, bis weniger als 20 mg im Blut sind: a*q^t = 20 => t = ln(20/a)/k\nt20 = np.log(20 / a) / k\nprint(f\"(d) Unter 20 mg nach {t20:.2f} h\")\n",
 "regression/korrelationskoeffizient-abc.py": "import numpy as np\n\nx = np.array([3, 1, 2])\ny = np.array([3, 2, 1])\n\nx_mean, y_mean = x.mean(), y.mean()\ns_xy = np.sum((x - x_mean) * (y - y_mean))\ns_xx = np.sum((x - x_mean) ** 2)\ns_yy = np.sum((y - y_mean) ** 2)\nr = s_xy / np.sqrt(s_xx * s_yy)\nprint(f\"s_xy={s_xy}, s_xx={s_xx}, s_yy={s_yy}\")\nprint(\"r =\", r)\n\n# Kontrolle\nprint(\"Kontrolle mit np.corrcoef:\", np.corrcoef(x, y)[0, 1])\n",
 "regression/modellwahl.py": "import numpy as np\nfrom scipy.optimize import brentq\n\nx = np.array([1, 2, 3, 4, 5, 6, 8, 10])\ny = np.array([20, 42, 52, 62, 68, 73, 80, 88])\n\ndef r(a, b):\n    return np.corrcoef(a, b)[0, 1]\n\n# (a) Empirischer Korrelationskoeffizient der vier Regressionstypen\nprint(\"(a) linear:       r =\", r(x, y))\nprint(\"    logarithmisch: r =\", r(np.log(x), y))\nprint(\"    exponentiell:  r =\", r(x, np.log(y)))\nprint(\"    Potenz:        r =\", r(np.log(x), np.log(y)))\n# Am besten passt die logarithmische Regression (r am naechsten bei 1).\n\n# (b) Logarithmisches Modell y = b*ln(x) + a\nb, a = np.polyfit(np.log(x), y, 1)\nprint(f\"(b) y = {a:.4f} + {b:.4f} * ln(x)\")\n\n# (c) Vorhersagen\nprint(f\"(c) nach 20 Tagen: {a + b * np.log(20):.2f} Wörter\")\nx100 = brentq(lambda xx: a + b * np.log(xx) - 100, 1, 1000)\nprint(f\"    100 Wörter nach {x100:.2f} Tagen\")\n\n# (d) Das logarithmische Modell wächst unbeschränkt -- unrealistisch für eine\n# Liste mit nur 100 Wörtern. Ein Modell mit Sättigung (z.B. logistisch) waere\n# hier realistischer.\n",
 "regression/r-rechteck.py": "import numpy as np\n\nx = np.array([0, 3, 0])\ny = np.array([0, 0, 3])\n\nr = np.corrcoef(x, y)[0, 1]\nprint(\"r =\", r)  # -1/2\n",
 "regression/regman.py": "import numpy as np\n\nx = np.array([0, 2, 3, 7])\ny = np.array([0, 3, 4, 5])\n\nm, b = np.polyfit(x, y, 1)\nprint(f\"Regressionsgerade: y = {m:.4f}x + {b:.4f}\")\n# y = 0.6538x + 1.0385, also y = 17/26 * x + 27/26\n\nresiduen = y - (m * x + b)\nprint(\"Summe Residuenquadrate:\", np.sum(residuen**2))  # 75/26 ≈ 2.8846\n",
 "regression/schaumkrone.py": "import numpy as np\nimport matplotlib.pyplot as plt\n\nt = np.array([0, 0.5, 1, 1.5, 2, 2.5, 3, 3.5, 4, 4.5, 5])\nh = np.array([90, 62, 43, 30, 21, 14, 10, 7, 5, 3, 2])\n\n# Wir nehmen h(t) = a * e^(k*t) an. Logarithmieren linearisiert das Problem:\n# ln(h) = k*t + ln(a), also lineare Regression mit (t, ln(h)).\nk, lna = np.polyfit(t, np.log(h), 1)\na = np.exp(lna)\nq = np.exp(k)  # Faktor pro Minute\n\nprint(f\"h(t) = {a:.2f} * e^({k:.2f} * t) = {a:.2f} * {q:.2f}^t\")\nprint(f\"Abnahme pro Minute: {(1 - q) * 100:.1f} %\")\n\nts = np.linspace(0, 5, 100)\nplt.scatter(t, h, color=\"red\", label=\"Messdaten\")\nplt.plot(ts, a * np.exp(k * ts), color=\"blue\", label=\"Regressionskurve\")\nplt.xlabel(\"Zeit [min]\")\nplt.ylabel(\"Schaumhöhe [mm]\")\nplt.legend()\nplt.show()\n",
 "regression/summen.py": "print(\"(a)\", sum(2 * i for i in range(1, 5)))\nprint(\"(b)\", sum(k**2 for k in range(2, 7)))\nprint(\"(c)\", sum(sum(i * k for k in range(2, 4)) for i in range(3, 6)))\n",
 "regression/virus.py": "import numpy as np\nimport matplotlib.pyplot as plt\nfrom scipy.optimize import curve_fit\n\nwoche = np.arange(1, 17)\nkranke = np.array(\n    [2, 4, 7, 17, 32, 60, 129, 254, 502, 979, 1862, 3365, 5614, 8076, 9645, 9986]\n)\n\ndef logistisch(x, G, A, c):\n    return G / (1 + A * np.exp(-c * x))\n\nparameter, _ = curve_fit(logistisch, woche, kranke, p0=(11000, 70000, 0.9))\nG, A, c = parameter\nprint(f\"(a) y = {G:.2f} / (1 + {A:.2f} * e^(-{c:.2f} * x))\")\n\nxs = np.linspace(1, 20, 300)\nplt.scatter(woche, kranke, color=\"red\", label=\"Messdaten\")\nplt.plot(xs, logistisch(xs, *parameter), color=\"blue\", label=\"Regressionskurve\")\nplt.axhline(G, color=\"grey\", linestyle=\"dashed\", label=f\"Kapazitätsgrenze G≈{G:.0f}\")\nplt.xlabel(\"Woche\")\nplt.ylabel(\"Kranke\")\nplt.legend()\nplt.show()\n\n# (b) G ist die Kapazitätsgrenze des Modells: mehr Kranke sind nie möglich.\nprint(f\"(b) Mehr als 11000 Kranke sind nie möglich (G ≈ {G:.2f}).\")\n",
 "regression/wetter-sin.py": "import numpy as np\nimport matplotlib.pyplot as plt\nfrom scipy.optimize import curve_fit\n\nmonat = np.arange(1, 13)\ntemperatur = np.array(\n    [1.6, 2.7, 6.6, 10.0, 14.2, 17.4, 19.7, 19.1, 15.1, 10.9, 5.5, 2.6]\n)\n\ndef modell(x, a, b, c, d):\n    return a * np.sin(b * x + c) + d\n\n# Ohne sinnvolle Startwerte findet curve_fit die Parameter nicht.\nparameter, _ = curve_fit(modell, monat, temperatur, p0=(9, 0.5, -2, 10))\na, b, c, d = parameter\nprint(f\"f(x) = {a:.2f} * sin({b:.2f} * x + {c:.2f}) + {d:.2f}\")\n\nxs = np.linspace(0, 13, 300)\nplt.scatter(monat, temperatur, color=\"red\", label=\"Messwerte\")\nplt.plot(xs, modell(xs, *parameter), color=\"blue\", label=\"Regressionskurve\")\nplt.xlabel(\"Monat\")\nplt.ylabel(\"Temperatur [°C]\")\nplt.legend()\nplt.show()\n\n# (b) Minimale Wachstumszeit: Anzahl Tage im Jahr mit Tagesmitteltemperatur > 10°C.\n# Wir tasten dazu ein feines Gitter von Tagen (umgerechnet in Monate) ab.\ntage_pro_jahr = 365\ntage = np.arange(tage_pro_jahr)\nmonate_der_tage = tage / (tage_pro_jahr / 12) + 1\nwarme_tage = np.sum(modell(monate_der_tage, *parameter) > 10)\nprint(f\"Tage über 10°C: {warme_tage} von {tage_pro_jahr}\")\n",
 "regression/zweivar.py": "import numpy as np\n\nx = np.array([1, 2, 3, 4, 5])\ny = np.array([2, 3, 5, 4, 6])\n\n# (a) Zwei-Variablen-Statistik\nn = len(x)\nx_mean, y_mean = x.mean(), y.mean()\nprint(f\"(a) n={n}, x̄={x_mean}, ȳ={y_mean}\")\nprint(f\"    Σx={x.sum()}, Σy={y.sum()}, Σxy={(x * y).sum()}, Σx²={(x**2).sum()}\")\n\n# (b) Steigung und y-Achsenabschnitt von Hand, mit s_xy und s_xx\ns_xy = (x * y).sum() - n * x_mean * y_mean\ns_xx = (x**2).sum() - n * x_mean**2\nm = s_xy / s_xx\nb = y_mean - m * x_mean\nprint(f\"(b) y = {m}x + {b}\")\n\n# (c) Kontrolle mit np.polyfit\nm_check, b_check = np.polyfit(x, y, 1)\nprint(f\"(c) Kontrolle: y = {m_check:.4f}x + {b_check:.4f}\")\n"
};
